<%- include('../partials/header', { title }) %>

<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Breed</th>
        </tr>
    </thead>
    <tbody>
            <tr>
                <td> <%= animal.name %></td>
                <td> <%= animal.breed %></td>
            </tr>
    </tbody>

</table>


<%- include('../partials/footer') %>